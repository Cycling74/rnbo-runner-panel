{
  "name": "rnbo-runner-panel",
  "version": "2.1.1-beta.12",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "dev": "concurrently 'cd ./client && npm run build-watch-dev' 'cd ./server && cargo run'",
    "changeset": "changeset",
    "lint": "cd client && npm run lint",
    "prepackage-debian": "npm run build && cd server && cross build --target armv7-unknown-linux-gnueabihf --release",
    "package-debian": "node scripts/package-linux.mjs --debian ./debian/ armv7-unknown-linux-gnueabihf",
    "prepackage-linux": "npm run build && cd server && cross build --target aarch64-unknown-linux-gnu --release",
    "package-linux": "node scripts/package-linux.mjs ./build/ aarch64-unknown-linux-gnu",
    "package-conan": "node scripts/package-conan.mjs",
    "preversion": "eslint ./client/src --report-unused-disable-directives --max-warnings 5",
    "postversion": "node scripts/apply-version.mjs",
    "release": "changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "concurrently": "^9.2.1"
  }
}

{
  "name": "@rnbo-runner-panel/server",
  "version": "2.2.0-beta.13",
  "scripts": {
    "predev": "mkdir -p ../client/out",
    "dev": "cargo run",
    "version": "node ../scripts/apply-version.mjs && git add ./Cargo.toml ./Cargo.lock"
  }
}
